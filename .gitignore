__pycache__/
*.py[cod]
*$py.class

.venv/
.venv-wsl/
venv/
env/
.env
.env.*
.claude/

# Personal Mode-2 config (your own input paths / default answers); never commit.
# The sanitized template config.env.example IS committed.
config.env
!config.env.example
# Legacy / alternate personal config names; never commit if a user keeps one.
config.json
config.ini

Logs/
Commands/
.Comments/
MediaReports/
ffmwiz-ffmpeg-reference.txt

*.log
*.tmp
*.temp
*.part
.coverage
.pytest_cache/
.tmp_tests/
.tmp_validation/
.mypy_cache/
.ruff_cache/
build/
.build/
dist/
*.egg-info/

# Local FFmpeg capability cache (environment-specific; safe to delete).
.cache/
ffmpeg_capabilities.json

output/
outputs/
encoded/

Thumbs.db
Desktop.ini
.DS_Store

# Private local-only documents; never commit.
explain-AI.md
/secrets.md
secrets.md

# AI assistant local config directories; never commit.
.kiro/
**/.kiro/
.codex/
**/.codex/
**/.claude/

# Local-only sandboxes named .ignoreme at any depth; never commit.
.ignoreme
.ignoreme/
**/.ignoreme

# Graphify knowledge-graph outputs and local edit/prompt reference; never commit.
graphify-out/
**/graphify-out/
reference.md

# Local-only project working-memory / lessons log; never commit.
lesson.md

# Portable AI context memory (.ai/) and CLAUDE.md; local-only, never commit.
/.ai/
**/.ai/
/CLAUDE.md

# Local-only temporary/scratch files and personal preview launchers; never commit.
_tmp*

# Hook Maker: local/private files (auto-managed)
/.env
/.env.*
!/.env.dist
!/.env.example
!/.env.sample
!/.env.template
/.agents/
/.claude/
/.cline/
/.codex/
/.cursor/
/.kiro/
/AGENTS.md
/explain-AI.md
/graphify-out/
/reference.md
/SKILL.md

!/.env.example
!/.env.sample
!/.env.template
!/.env.dist

/skills-lock.json

# Local CI executor: binaries, credentials and a full working checkout.
/.actions-runner/
